const mock = require("./mock");

describe("Jest mock", () => {
  // Limpar todas as funções simuladas após cada teste

  it("Função mock", () => {});

  it("Função original (irá retorna erro)", () => {});
});
